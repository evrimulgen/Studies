#include <cstdlib>
#include <iostream>
#include <vector>

using namespace std;

int main()
{
  vector< vector<int> > L(10, vector<int> () );
  int s=31;
  for(int i=0;i<10;i++)
    {
      L[1].push_back(s--);
    }

  while(!L[1].empty())
    {
      cout<<
    }
  int k=L[1].pop_back
  return 0;
}
