#!/usr/bin/env python3

liste={{
er=re.compile("^(\w)+.*[A-Z][a-z]{2} \s+ ([A-Z][a-z]{2})\s + ([0-9]{1,2})")
f=os.popen("last","r")

for ligne in f.readlignes():
   res=er.serach

   if res :
       if res.group(1) in kiste.keys():
               user=liste[res.group(1)]
                if res.group(2) in user.keys():
                     mois=user[res.group(2)]
                 if res.group(3) in mois.keys():
                     
                 
