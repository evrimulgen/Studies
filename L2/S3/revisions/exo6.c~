#include<stdlib.h>
#include<stdio.h>

int main(int argc,char* argv[]){
  int max=atoi(argv[1]),min=atoi(argv[2]);

  for(int i=0;i<argc;i++){
    if (max < atoi(argv[i])max=atoi(argv[i]);
    if (min > atoi(argv[i])min=atoi(argv[i]);
	}


	return 0; }
